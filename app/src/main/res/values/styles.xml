<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textSize">@dimen/font_size_default</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextViewLabelMovieDetails">
        <item name="android:textColor">@color/cinza_text</item>
        <item name="android:layout_marginBottom">@dimen/margin_default</item>
    </style>

    <style name="TextViewValueMovieDetails">
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginBottom">@dimen/margin_default</item>
    </style>

</resources>
